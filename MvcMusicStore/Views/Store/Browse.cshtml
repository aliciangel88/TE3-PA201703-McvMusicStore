@model MvcMusicStore.Models.Genero

@{
    ViewBag.Title = "Browse";
}

<h2>Browsing Genero: @Model.Name</h2>

<ul>
@foreach (var album in Model.Albums) { 
    <li>
        @Html.ActionLink(album.Title, "Details", new {id = album.ArtistId})
        </li>
    }

</ul>